// create table 
>> create table table_name(
    id int primary key,
    name varchar(100),
    age int ,
);

// SHOW TABLES ALL DATA
>> SELECT * FROM TABLENAME;

// INSERT DATA
>> INSERT INTO TABLENAME (ID,NAME,AGE) VALUES (1,'PRAVIN',22);

// INSERT MULTIPLE DATA
>> INSERT INTO TABLENAME (ID,NAME,AGE) VALUES
VALUES
(2,'SAHIL',23),
(2,'SAHIL',23),
(2,'SAHIL',23);

>> SELECT DATA

>> SELECT COLUMNS (SPECIFIC) DATA

>> SELECT ID,NAME FROM TABLENAME;

>> SELECT ALL COLUMNS DATA

>> SELECT * FROM TABLENAME;

>> ALTER  - IS USED TO ADD,DELETE,MODIFY COLUMNS IN AN EXISTING TABLENAME

>> The ALTER TABLE statement is also used to add and drop various constraints on an existing table.

>> TO CHANGE THE DATA TYPE OF COLUMNN OR SIZE OF COLUMNN 

>> ALTER TABLE TABLENAME 
ALTER COLUMN NAME TYPE VARCHAR(300);

>> DROP COULMN

>> ALTER TABLE TABLENAME
DROP COLUMN COLUMNNAME;


>> ADD COLUMNS

>> ALTER TABLE TABLENAME 
ADD COULMN NME VARCHAR(100);
ADD COULMN AGE INT;

>> UPDATE - IS USED TO MODIFY THE VALUES IN EXISTING TABLE 

>> UPDATE TABLENAME
SET NAME = 'ABC' 
WHERE ID = 1;

>> Update Multiple Columns

>> UPDATE TABLENAME
SET NAME = 'XYZ', AGE = 25
WHERE ID = 1;

>> DELETE - IS USED TO DELETE THE ROWS FROM THE TABLE

>> DELETE FROM TABLENAME
WHERE ID = 1;

>> DELETE ALL ROWS

>> DELETE FROM TABLENAME;


-----------------------------
..
Sort Data
The ORDER BY keyword is used to sort the result in ascending or descending order.

The ORDER BY keyword sorts the records in ascending order by default. To sort the records in descending order, use the DESC keyword.

--------------------------------

Sort the table by price:

SELECT * FROM products
ORDER BY price;

-------------------------

DESC
The ORDER BY keyword sorts the records in ascending order by default. To sort the records in descending order, use the DESC keyword.

Example
Sort the table by price, in descending order:

SELECT * FROM products
ORDER BY price DESC;

--------------------------------


